/**
 * Represents a hero section message displayed on the home page.
 */
export class HeroMessage {
  constructor(
    public readonly idMessage:  number,
    public readonly text:       string,
  ) {}
}
